This was all done locally on mysql

The sql database after all the retrieves, updates, and deletes --> assignment2_endtable
The sql database before inserts --> assignment2_emptytable
The view can be found in *_endtable
Unsure where triggers are, but they should be there

The folders are the results of exporting from mysql

Felix Yang
CS3200
